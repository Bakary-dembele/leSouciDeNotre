<form method="post" action="{{ path('foot_supprimer', {'id': evenement_foot.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet événement?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evenement_foot.id) }}">
    <button class="btn">Delete</button>
</form>
