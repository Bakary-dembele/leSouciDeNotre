{% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
    <div class="creer_maraude">
        <div class="container" id="conponent_creer_collecte">
            <div class="row">
                <div class="col-12 sm-12 md-12 col-lg-">
                    <h2 class="text-center">Ajouter un utilisateur</h2>
                    {{ form_start(form) }}
                            {{ form_row(form.nom) }}
                            {{ form_row(form.prenom) }}
                            {{ form_row(form.telephone) }}
                            {{ form_row(form.email) }}
                            {{ form_row(form.password) }}
                           {{ form_row(form.groupe) }}
                            {{ form_row(form.roles) }}
                        </div>
                <div class="d-flex justify-content-center col-12 sm-12 md-12 col-lg-">
                    <div class="">
                        <a {{ path('users_index') }}">
                        <button type="submit" class="btn btn-success " formnovalidate>Enregistrer</button>
                        </a>
                        {{ form_end(form) }}
                    </div>
                    <div class="">
                        <a href="{{ path('users_index') }}" class="btn btn-danger ">Retour</a>
                    </div>
                </div>
            </div>
        </div>

    </div>
    </div>

{% endblock %}
