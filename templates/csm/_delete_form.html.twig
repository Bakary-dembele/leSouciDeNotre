<form method="post" action="{{ path('csm_delete', {'id': csm.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ csm.id) }}">
    <button class="btn">Delete</button>
</form>
