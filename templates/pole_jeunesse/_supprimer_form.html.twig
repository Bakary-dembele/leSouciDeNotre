<form method="post" action="{{ path('pole_jeunesse_delete', {'id': pole_jeunesse.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pole_jeunesse.id) }}">
    <button class="btn">Delete</button>
</form>
